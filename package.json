{
  "name": "the-hindu-pdf-daily",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html style.css script.js dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}
